# Embedding Configuration (shared between embedding-service and aas-weaviate-mcp)
# 
# IMPORTANT: Both services MUST use the same embedding model!
# Vectors from different models are incompatible.

# Embedding model in provider:model format
# Examples:
#   openai:text-embedding-3-small
#   ollama:nomic-embed-text
#   google_genai:text-embedding-004
#   voyageai:voyage-3
EMBEDDING_MODEL=openai:text-embedding-3-small

# API Keys (set only what you need for your provider)
OPENAI_API_KEY=
GOOGLE_API_KEY=
VOYAGE_API_KEY=

# Ollama (local, no API key needed)
OLLAMA_HOST=http://host.docker.internal:11434
